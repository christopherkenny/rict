template_var_map <- function() { # nocov start
  'A `redist_map` or `sf` object.'
} # nocov end

template_var_plan <- function() { # nocov start
  'Column in `map` or vector of district assignments.'
} # nocov end

template_var_as_gt <- function() { # nocov start
  'Logical. Should output be a `gt` table? Default: `TRUE`.'
} # nocov end
